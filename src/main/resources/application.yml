server:
  port: ${PORT:8080}

spring:
  application:
    name: auth
  datasource:
    driver-class-name: org.postgresql.Driver
  jpa:
    database: postgresql
    hibernate:
      ddl-auto: validate
  # Only use Cloud SQL in Prod.
  cloud:
    gcp:
      sql:
        enabled: false
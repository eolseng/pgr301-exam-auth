server:
  port: ${PORT:8080}

spring:
  application:
    name: auth
  cloud:
    gcp:
      sql:
        # Disable by default. Enabled in 'prod'.
        enabled: false
management:
  metrics:
    export:
      influx:
        # Disable by default. Enabled in 'dev'.
        enabled: false
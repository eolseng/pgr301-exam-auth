# Connect to GCP Cloud SQL Instance in prod environment
spring:
  datasource:
    initialization-mode: always
  cloud:
    gcp:
      sql:
        enabled: true

# Disable InfluxDB metric export in 'prod' environment
management:
  metrics:
    export:
      influx:
        enabled: false